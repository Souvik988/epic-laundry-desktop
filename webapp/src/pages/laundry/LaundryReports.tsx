import { useQuery } from '@tanstack/react-query'
import { BarChart3, CircleDollarSign, Loader2, Users, WalletCards } from 'lucide-react'
import { useState } from 'react'
import { apiGet } from '@/lib/api'
import { formatINR } from '@/lib/utils'

type Reports = { summary: { orderValue: number; collected: number; outstanding: number; expenses: number; operatingCash: number; orders: number; customers: number }; stateBreakdown: Array<{ state: string; count: number; amount: number }>; paymentBreakdown: Array<{ paymentMode: string; count: number; amount: number }> }

export default function LaundryReports() { const [from, setFrom] = useState(''); const [to, setTo] = useState(''); const report = useQuery({ queryKey: ['laundry-reports', from, to], queryFn: () => apiGet<Reports>(`/laundry/reports?${from ? `from=${from}&` : ''}${to ? `to=${to}` : ''}`) }); if (report.isLoading) return <div className="grid h-80 place-items-center"><Loader2 className="h-6 w-6 animate-spin text-[#3a7d78]" /></div>; if (report.isError || !report.data) return <div className="rounded-2xl border border-rose-200 bg-rose-50 p-6 text-rose-800">Reports could not be loaded.</div>; const data = report.data; return <div className="animate-in fade-in slide-in-from-bottom-2 duration-500"><div className="flex flex-col gap-3 md:flex-row md:items-end md:justify-between"><div><p className="text-[10px] font-bold uppercase tracking-[.18em] text-[#4d8982]">Performance ledger</p><h1 className="mt-1 font-serif text-3xl text-[#17353c]">Reports</h1><p className="mt-1 text-sm text-[#718087]">Collections, order value, payments, and posted store expenses.</p></div><div className="flex gap-2"><label className="text-xs text-[#617178]">From<input type="date" value={from} onChange={(event) => setFrom(event.target.value)} className="mt-1 block h-9 rounded-lg border border-[#263f44]/15 bg-white px-2" /></label><label className="text-xs text-[#617178]">To<input type="date" value={to} onChange={(event) => setTo(event.target.value)} className="mt-1 block h-9 rounded-lg border border-[#263f44]/15 bg-white px-2" /></label></div></div><section className="mt-6 grid gap-4 sm:grid-cols-2 xl:grid-cols-4"><Metric label="Order value" value={formatINR(data.summary.orderValue)} icon={BarChart3} /><Metric label="Collected" value={formatINR(data.summary.collected)} icon={CircleDollarSign} /><Metric label="Store expenses" value={formatINR(data.summary.expenses)} icon={WalletCards} /><Metric label="Operating cash" value={formatINR(data.summary.operatingCash)} icon={Users} /></section><section className="mt-5 grid gap-5 xl:grid-cols-2"><Breakdown title="Order lifecycle" headers={['State', 'Orders', 'Value']} rows={data.stateBreakdown.map((row) => [row.state, String(row.count), formatINR(row.amount)])} /><Breakdown title="Payment mix" headers={['Method', 'Orders', 'Value']} rows={data.paymentBreakdown.map((row) => [row.paymentMode, String(row.count), formatINR(row.amount)])} /></section><div className="mt-5 rounded-[22px] bg-[#123039] p-5 text-[#edf3ec]"><p className="text-xs uppercase tracking-[.16em] text-[#b8d3c8]">Customer & balance snapshot</p><div className="mt-3 flex flex-wrap gap-x-10 gap-y-3"><span><strong className="font-serif text-2xl text-[#f1ca75]">{data.summary.orders}</strong><span className="ml-2 text-sm">orders in range</span></span><span><strong className="font-serif text-2xl text-[#f1ca75]">{data.summary.customers}</strong><span className="ml-2 text-sm">customers served</span></span><span><strong className="font-serif text-2xl text-[#f1ca75]">{formatINR(data.summary.outstanding)}</strong><span className="ml-2 text-sm">outstanding</span></span></div></div></div> }
function Metric({ label, value, icon: Icon }: { label: string; value: string; icon: typeof BarChart3 }) { return <div className="rounded-[20px] border border-[#263f44]/10 bg-white p-5 shadow-[0_8px_28px_rgba(37,48,43,.04)]"><Icon className="h-5 w-5 text-[#3a7d78]" /><p className="mt-4 text-xs font-bold uppercase tracking-[.13em] text-[#718087]">{label}</p><p className="mt-1 font-serif text-3xl text-[#17353c]">{value}</p></div> }
function Breakdown({ title, headers, rows }: { title: string; headers: string[]; rows: string[][] }) { return <section className="overflow-hidden rounded-[22px] border border-[#263f44]/10 bg-white shadow-[0_8px_28px_rgba(37,48,43,.04)]"><h2 className="border-b border-[#263f44]/10 p-5 font-serif text-xl text-[#17353c]">{title}</h2><table className="w-full text-left text-sm"><thead className="bg-[#fafaf7] text-[10px] font-bold uppercase tracking-[.14em] text-[#718087]"><tr>{headers.map((header) => <th key={header} className="px-5 py-3">{header}</th>)}</tr></thead><tbody>{rows.map((row) => <tr key={row[0]} className="border-t border-[#263f44]/8">{row.map((cell, index) => <td key={index} className="px-5 py-3.5"><span className={index === 2 ? 'font-bold tabular-nums' : ''}>{cell}</span></td>)}</tr>)}</tbody></table></section> }
