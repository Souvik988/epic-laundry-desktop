import { Link, useParams } from 'react-router-dom'
import { useQuery } from '@tanstack/react-query'
import { ArrowLeft, Download, Loader2, Printer, RefreshCw } from 'lucide-react'
import { useState } from 'react'
import { apiGet } from '@/lib/api'
import { formatINR } from '@/lib/utils'

const reports = [
  ['invoice', 'Invoice Report'], ['collection', 'Collection Report'], ['order', 'Order Report'], ['consolidated-invoices', 'Consolidated Invoices'], ['customer', 'Customer Report'], ['customer-package', 'Customer Package Report'], ['customer-list', 'Customer List'], ['growth', 'Growth Report'], ['discount', 'Discount Report'], ['expense', 'Expense Report'], ['balance', 'Balance Report'], ['pickup', 'Pickup Overview'], ['rider-delivery', 'Rider Delivery'], ['rider-collection', 'Rider Collection'], ['warehouse-user-work', 'Warehouse User Work Report'],
] as const
type Detail = { kind: string; from: string | null; to: string | null; columns: string[]; rows: Array<Record<string, unknown>>; totalRows: number }

export default function LaundryReportDetail() {
  const { kind = 'invoice' } = useParams(); const meta = reports.find(([id]) => id === kind) || reports[0]; const [from, setFrom] = useStateValue(); const [to, setTo] = useStateValue(); const [search, setSearch] = useStateValue()
  const query = useQuery({ queryKey: ['laundry-report-detail', meta[0], from, to, search], queryFn: () => apiGet<Detail>(`/laundry/reports/${meta[0]}?${from ? `from=${from}&` : ''}${to ? `to=${to}&` : ''}${search ? `search=${encodeURIComponent(search)}` : ''}`) })
  const data = query.data
  return <div className="animate-in fade-in slide-in-from-bottom-2 duration-500"><div className="flex flex-col gap-3 lg:flex-row lg:items-end lg:justify-between"><div><Link to="/laundry/reports" className="inline-flex items-center gap-1 text-xs font-bold text-[#39786f]"><ArrowLeft className="h-3.5 w-3.5" />Reports overview</Link><p className="mt-4 text-[10px] font-bold uppercase tracking-[.18em] text-[#4d8982]">Verified report view</p><h1 className="mt-1 font-serif text-3xl text-[#17353c]">{meta[1]}</h1><p className="mt-1 text-sm text-[#718087]">Server-backed rows for the active store and selected date range.</p></div><div className="flex flex-wrap items-end gap-2"><label className="text-xs font-semibold text-[#617178]">From<input type="date" value={from} onChange={(event) => setFrom(event.target.value)} className="mt-1 block h-9 rounded-lg border border-[#263f44]/15 bg-white px-2 font-normal" /></label><label className="text-xs font-semibold text-[#617178]">To<input type="date" value={to} onChange={(event) => setTo(event.target.value)} className="mt-1 block h-9 rounded-lg border border-[#263f44]/15 bg-white px-2 font-normal" /></label><input value={search} onChange={(event) => setSearch(event.target.value)} placeholder="Filter rows" className="h-9 w-40 rounded-lg border border-[#263f44]/15 bg-white px-3 text-xs" /><button type="button" onClick={() => query.refetch()} className="inline-flex h-9 items-center gap-1.5 rounded-lg border border-[#263f44]/15 bg-white px-3 text-xs font-bold text-[#315d57]"><RefreshCw className="h-3.5 w-3.5" />Refresh</button><button type="button" onClick={() => window.print()} className="inline-flex h-9 items-center gap-1.5 rounded-lg border border-[#263f44]/15 bg-white px-3 text-xs font-bold text-[#315d57]"><Printer className="h-3.5 w-3.5" />Print</button><button type="button" disabled={!data?.rows.length} onClick={() => data && void exportRows(meta[1], data)} className="inline-flex h-9 items-center gap-1.5 rounded-lg bg-[#123039] px-3 text-xs font-bold text-white disabled:bg-[#a8b7b2]"><Download className="h-3.5 w-3.5" />Excel</button></div></div><div className="mt-6 grid gap-5 xl:grid-cols-[220px_minmax(0,1fr)]"><nav className="h-fit rounded-[22px] border border-[#263f44]/10 bg-white p-2 shadow-[0_8px_28px_rgba(37,48,43,.04)]">{reports.map(([id, label]) => <Link key={id} to={`/laundry/reports/${id}`} className={`block rounded-xl px-3 py-2.5 text-xs font-semibold ${id === meta[0] ? 'bg-[#eaf3ef] text-[#205d55]' : 'text-[#617178] hover:bg-[#f5f7f3]'}`}>{label}</Link>)}</nav><section className="overflow-hidden rounded-[22px] border border-[#263f44]/10 bg-white shadow-[0_8px_28px_rgba(37,48,43,.04)]">{query.isLoading ? <div className="grid h-80 place-items-center"><Loader2 className="h-6 w-6 animate-spin text-[#3a7d78]" /></div> : query.isError || !data ? <p className="p-8 text-center text-rose-700">This report could not be loaded.</p> : <><div className="flex items-center justify-between border-b border-[#263f44]/10 bg-[#fafaf7] px-5 py-4"><span className="text-xs text-[#617178]"><strong className="text-[#315d57]">{data.totalRows}</strong> rows · {data.from || 'all dates'}{data.to ? ` to ${data.to}` : ''}</span><span className="text-[10px] font-bold uppercase tracking-[.14em] text-[#829092]">Authoritative local data</span></div><div className="overflow-x-auto"><table className="w-full min-w-[720px] text-left text-sm"><thead className="bg-[#fbfbf8] text-[10px] font-bold uppercase tracking-[.13em] text-[#718087]"><tr>{data.columns.map((column) => <th key={column} className="px-4 py-3">{labelize(column)}</th>)}</tr></thead><tbody>{data.rows.length ? data.rows.map((row, index) => <tr key={index} className="border-t border-[#263f44]/8">{data.columns.map((column) => <td key={column} className="px-4 py-3.5 text-[#40565a]">{formatCell(row[column], column)}</td>)}</tr>) : <tr><td colSpan={Math.max(data.columns.length, 1)} className="py-16 text-center text-[#718087]">No rows match this report.</td></tr>}</tbody></table></div></>}</section></div></div>
}

function useStateValue() { return useState('') }
function labelize(value: string) { return value.replace(/([A-Z])/g, ' $1').replace(/_/g, ' ').replace(/^./, (char) => char.toUpperCase()) }
function formatCell(value: unknown, column: string) { if (value === null || value === undefined || value === '') return '—'; if (typeof value === 'number' && /(amount|total|revenue|tax|discount|balance|value|expense|collected)/i.test(column)) return formatINR(value); if (typeof value === 'object') return Array.isArray(value) ? value.join(', ') : JSON.stringify(value); return String(value) }
async function exportRows(title: string, data: Detail) { const XLSX = await import('xlsx'); const sheet = XLSX.utils.json_to_sheet(data.rows); const workbook = XLSX.utils.book_new(); XLSX.utils.book_append_sheet(workbook, sheet, title.slice(0, 31)); XLSX.writeFile(workbook, `${title.toLowerCase().replace(/[^a-z0-9]+/g, '-')}-${new Date().toISOString().slice(0, 10)}.xlsx`, { compression: true }) }
