# V4 execution matrix

Authoritative checkpoint: local `main` at `bebb4fb` (2026-09-02). Remote `origin/main` remains `7276726`; no V4 work has been pushed.

| Requirement group | Current implementation / evidence | Class | DB / API / UI / test | Remaining action |
| --- | --- | --- | --- | --- |
| Offline desktop foundation | Electron loopback Fastify, SQLite/WAL, workspace isolation, restart/recovery, tag/production/cash workflows exist. `bebb4fb` fixed first-run parent directory handling. | VERIFIED_CURRENT | Store migration 20; local self-tests | Re-run clean CI after a permitted remote update. |
| Legacy internal outbox | Generic records-backed outbox marks an event published on read. | PARTIAL | `kernel/event-bus.ts` | Preserve for legacy internal use; build separate ACK-based sync tables/relay. |
| Sync inbox, outbox, checkpoints, retries | Dedicated SQLite tables now retain Pending/InFlight/Acknowledged/Retry/DeadLetter delivery, durable inbox dedupe, checkpoints, deterministic backoff, lease recovery and manual held-event replay. The legacy records-backed outbox remains isolated. | EPIC_EXTENSION | migrations 21–22; `modules/marketplace/edge-sync.ts`; API; `test:marketplace-sync` | Connect a real cloud transport only when an authoritative control-plane contract/credentials exist; add operator dead-letter remediation UI. |
| Device identity / machine auth | Store-scoped device record, Ed25519 enrollment material, credential reference (not secret), revocation state and configuration gate now exist. | PARTIAL | migration 21; edge-sync module; sync status API | Protected OS-keychain storage, rotation flow and cloud-issued short-lived credentials require the real marketplace control plane. |
| Marketplace contract / simulator | Adjacent `Lndry_backend` is reference only; no accepted edge contract. A deterministic `sim://` development simulator now exercises cloud-like receipt dedupe and inbound queues without pretending to be production. | PARTIAL | `modules/marketplace/simulator.ts`; `MARKETPLACE_EVENT_CONTRACTS.md` | Agree and validate signed remote schemas, authentication and cursor contracts with the marketplace backend. |
| External order identity / online queue | `order_external_links` and versioned marketplace order projections now provide idempotent cross-channel identity, ordered inbound application, accept/reject commands and a paged owner queue. | PARTIAL | migration 21; `/api/marketplace/orders`; web Online orders; sync/API tests | Link an accepted projection to the existing physical intake/operational order and implement reassessment/customer approval. |
| Availability / capacity | Existing local operational capacity is limited to station capacity; no marketplace projection. | PARTIAL | Store settings / laundry domain | Add vendor/store availability and outbound projection. |
| Request, intake, reassessment, approval | Existing booking produces operational order; online request separation absent. | MISSING | — | Add immutable request/assessment/approval lifecycle. |
| Marketplace payments / cash / settlement | Existing local payment, wallet, cash close and reconciliation work is preserved; no marketplace policy/settlement model. | PARTIAL | finance tables/modules | Add versioned policy, allocation and settlement ledger. |
| Tax engine | Fixed-scale money exists, but quote tax is global and supplier settings have defaults. | PARTIAL | `money.ts`, GST modules | P0: line tax snapshots, profile/readiness, FY sequence and canonical invoice. |
| E-invoice / GSP | Existing GSP module exists; production applicability and provider evidence are absent. | NEEDS_PROVIDER / NEEDS_LEGAL_VALIDATION | GST module | Add explicit capability states and fixtures without fabricated IRN. |
| Invoice / documents | Shared print/PDF foundation exists; no immutable canonical V4 invoice snapshot. | PARTIAL | print renderer / GST module | Build snapshot and derived documents. |
| Garment visual identity | V4 migration 20 and UI use explicit visual keys; static visual audit/contact sheet absent. | PARTIAL | migration 20, catalogue/booking | Add audit command, contact sheet, optimized assets and runtime review. |
| Design system / responsive UI | Mixed visual tokens and large route-specific styles remain. | PARTIAL | webapp | Establish semantic tokens/primitives; QA at required widths. |
| Privacy / security | RBAC, loopback binding, redacted diagnostics and backups exist. Marketplace replay/device/webhook privacy controls absent. | PARTIAL | auth / diagnostics | Implement sync machine auth, replay protection, scoped API, privacy workflows. |
| Performance | Some normalized projections/indexes exist. No representative benchmark fixture/report. | NEEDS_VERIFICATION | store indexes | Add load fixtures, measurements and pagination/virtualization remediation. |
| Release | Internal unsigned build distinction and signature verification exist. New Windows CI job has not run remotely; production signing/update feed unavailable. | PENDING_REMOTE_CI / EXTERNAL_BLOCKER | `.github/workflows/ci.yml`, desktop | Test local packaging/launch where possible; retain truthful production gate. |
| Hardware / providers | Keyboard scanner/native print intents exist. | NEEDS_REAL_HARDWARE / NEEDS_PROVIDER | hardware module | Keep evidence-based state; validate with real equipment/credentials. |

Latest local evidence: `server` typecheck/build plus clean DB bootstrap, migration, marketplace sync and marketplace API self-tests passed; `webapp` production build passed. This is local evidence only—remote CI remains pending because no V4 branch was pushed.

Execution order: (1) finish clean baseline evidence, (2) availability + request/intake/reassessment, (3) settlement, (4) tax/invoice, (5) UI/document/runtime/performance/security audit, (6) release evidence.
