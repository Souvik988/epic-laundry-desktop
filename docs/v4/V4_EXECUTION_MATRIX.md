# V4 execution matrix

Authoritative checkpoint: local `main` at `008c1ea` (2026-09-03). Remote `origin/main` remains `7276726`; no V4 work has been pushed.

| Requirement group | Current implementation / evidence | Class | DB / API / UI / test | Remaining action |
| --- | --- | --- | --- | --- |
| Offline desktop foundation | Electron loopback Fastify, SQLite/WAL, workspace isolation, restart/recovery, tag/production/cash workflows exist. `bebb4fb` fixed first-run parent directory handling. | VERIFIED_CURRENT | Store migration 20; local self-tests | Re-run clean CI after a permitted remote update. |
| Legacy internal outbox | Generic records-backed outbox marks an event published on read. | PARTIAL | `kernel/event-bus.ts` | Preserve for legacy internal use; build separate ACK-based sync tables/relay. |
| Sync inbox, outbox, checkpoints, retries | Dedicated SQLite tables now retain Pending/InFlight/Acknowledged/Retry/DeadLetter delivery, durable inbox dedupe, payload-collision rejection, checkpoints, deterministic backoff, lease recovery and manual held-event replay. Shared envelope validation rejects missing identity, invalid timestamps, wrong event families, invalid versions and non-object financial payloads before application. The legacy records-backed outbox remains isolated. A dedicated owner/support Sync Status cockpit now exposes connection mode, device identity, checkpoint timing, queue counts, held/failed inbound events, and online-order projections without claiming a remote resync exists. | EPIC_EXTENSION | migrations 21–22; `modules/marketplace/edge-sync.ts`; `/api/marketplace/sync/status`; `LaundrySyncStatus.tsx`; `test:marketplace-sync` | Connect a real cloud transport only when an authoritative control-plane contract/credentials exist; add authenticated operator replay/remediation controls once the cloud contract defines them. |
| Device identity / machine auth | Store-scoped device record, Ed25519 enrollment material, credential reference (not secret), revocation state and configuration gate now exist. | PARTIAL | migration 21; edge-sync module; sync status API | Protected OS-keychain storage, rotation flow and cloud-issued short-lived credentials require the real marketplace control plane. |
| Marketplace contract / simulator | Adjacent `Lndry_backend` is reference only; no accepted edge contract. A deterministic `sim://` development simulator now exercises device-scoped receipt dedupe, network outage/retry, inbound order/payment/settlement queues, duplicate delivery, approval/rejection-compatible events and local application without pretending to be production. | PARTIAL | `modules/marketplace/simulator.ts`; edge-sync handlers; `MARKETPLACE_EVENT_CONTRACTS.md`; sync self-test | Agree and validate signed remote schemas, authentication and cursor contracts with the marketplace backend. |
| External order identity / online queue | `order_external_links` and versioned marketplace order projections now provide idempotent cross-channel identity, ordered inbound application, accept/reject commands, a paged owner queue, and an explicit validated link to an existing local laundry order. Accepted requests with complete garment/service facts now materialize exactly once into the existing local booking aggregate; incomplete estimates remain explicitly intake-required. Materialization is blocked with `TAX_PROFILE_INCOMPLETE` until the store has an explicit supplier profile. The Online Orders route now provides a responsive queue cockpit, search/filter controls, request detail, rejection reason, physical-intake line entry, and materialization action. Queue visibility uses `orders.read`; all mutations remain `orders.edit`-protected and read-only roles receive an explicit notice. The main store-order screen now uses SQL-backed bounded pagination and server-side search. | EPIC_EXTENSION / PARTIAL | migration 21; `/api/marketplace/orders`; `/materialize`; `LaundryOnlineOrders.tsx`; `LaundryOrders.tsx`; `test:marketplace-cross-channel`; runtime QA 1024/1366/1440/1920 | Add pickup scheduling, customer approval/timeline integration, and richer completed-order work-card linking. |
| Availability / capacity | Store-scoped marketplace availability now persists state, hours, holidays, zones, capabilities, lead time, orders/bags/kg/stops capacity and stale threshold. | PARTIAL | migration 23; availability module/API | Publish the projection through the real marketplace gateway and update device heartbeat on live transport. |
| Request, intake, reassessment, approval | Request, intake, reassessment and approval records now preserve original estimates and actuals; material deltas require approval and are idempotent. Approved actual intake can now materialize into the existing physical operational order without overwriting the original request. | EPIC_EXTENSION / PARTIAL | order-truth module/API; edge materialization; `test:marketplace-cross-channel` | Connect customer approval/timeline events and complete the richer operator intake/work-card surface. |
| Marketplace payments / cash / settlement | Separate paise-based settlement policy engine records transparent vendor gross, promotions, commission, fees, withholding and adjustments; customer collection/refund now reconcile as their own paise line equation with immutable allocation records; cash collection remains distinct; provider payment events have a fail-closed HMAC webhook adapter with duplicate delivery handling; every persisted settlement now creates an immutable, idempotent Marketplace Settlement Statement with a print route and explicit no-tax-invoice/no-payout-success disclaimer. | PARTIAL | settlements/settlement-statement/provider-events modules/API; self-tests | Add provider-to-order mapping and provider-backed reconciliation through the real control plane; normalize settlement batches/payout attempts when cloud contracts exist. |
| Marketplace notifications | Channel-independent notification events can be queued once per event/channel and delivery can advance only with provider message evidence; failed delivery requires a reason. | PARTIAL | notifications module/API; self-tests | Connect lifecycle events and real push/WhatsApp/SMS/email providers; retain provider callbacks and expose operator retry diagnostics. |
| Tax engine | Canonical fixed-scale engine supports line-level SAC/HSN, discounts, intra-state CGST/SGST, inter-state IGST and exact reconciliation. An effective-dated, approval-gated classification master now resolves approved rules by transaction date. | PARTIAL | `gst/canonical-tax.ts`, `gst/tax-policy.ts`; API; self-tests | Replace legacy global quote authority and obtain accountant/legal approval for production classifications and applicability. |
| Store/vendor tax profile | Persisted store-scoped supplier profile now validates legal identity, address, state code, pincode, registration/GSTIN and explicit e-invoice capability state. Readiness fails for missing profiles and, for registered suppliers, missing effective approved classifications; unregistered profiles remain explicitly operational without GST rules. Production entry-point invoice/posting paths now fail closed without the profile; the desktop no longer injects state 29. | PARTIAL | `gst/tax-policy.ts`; `/api/gst/tax-profile`, `/api/gst/tax-readiness`; self-test | Connect profile to all remaining ERP document paths and validate the company's actual legal/tax model. |
| E-invoice / GSP | IRN generation now requires the stored registered supplier profile and `Ready`/`Sandbox` capability, uses the canonical laundry snapshot when linked, and labels sandbox output as `SANDBOX_GENERATED`; production applicability and provider evidence remain absent. | NEEDS_PROVIDER / NEEDS_LEGAL_VALIDATION | `gst/irn-service.ts`; GST module; invoice self-test | Connect a real provider under approved legal applicability and retain real IRP evidence without treating sandbox output as production. |
| Invoice / documents | Immutable canonical invoice snapshot now stores FY series, supplier/customer, tax snapshot, paid and outstanding amounts; configured-store laundry booking and POS invoices create/link it, POS uses explicit Product/HSN metadata unless a service/SAC mapping is declared, recurring invoices with approved tax evidence are bridged automatically, local collections/reversals create immutable Payment Receipt/Refund Receipt snapshots, persisted marketplace settlements create immutable Marketplace Settlement Statement snapshots, applicability-approved debit notes now have immutable FY-numbered snapshots, the legacy tax print and IRN routes resolve through canonical evidence, and cancellation/supersession creates a submitted credit note plus canonical credit-note snapshot without double-reversing the invoice. | PARTIAL | invoice-snapshot/legacy-invoice-bridge/canonical-receipts/settlement-statement/cancellation-credit-note/canonical-invoice-print modules/API; self-tests | Route all remaining legacy GSTR/debit-note paths through the snapshot and add marketplace/provider receipt evidence and settlement-batch/payout documents when contracts exist. |
| Garment visual identity | V4 migration 20 and UI use explicit visual keys; migrations 24–29 correct thirteen active semantic mismatches and migrate all active runtime visual paths to reviewed 256px WebP derivatives. `audit:garment-assets` scans active rows, validates taxonomy and packaged paths, and classifies exact/shared/custom/missing/broken states. The 32-row contact sheet now reports zero delivery-budget warnings. | PARTIAL | migrations 20, 24–29; `modules/laundry/garment-assets.ts`; `CATALOGUE_MEDIA_CONTACT_SHEET.html`; audit/self-tests | Replace the remaining six intentional closely-related garment decisions with reviewed dedicated visuals. |
| Design system / responsive UI | Mixed visual tokens and large route-specific styles remain. Online Orders now establishes a deliberate high-density cockpit pattern with semantic status accents, queue/detail composition, accessible controls, and no horizontal overflow at 1024/1366/1440/1920px. | PARTIAL / EPIC_EXTENSION | webapp; `DESIGN_SYSTEM.md`; runtime screenshots and viewport checks | Continue token/primitives extraction across legacy routes and complete typography/media QA. |
| POS quantity UX | Backend enforces whole-number Piece/Pair quantities; booking UI now uses integer controls for Piece/Pair and decimal inputs with unit-aware steps for Kilogram/Square Foot. Runtime inspection confirmed bulk lines render as unit-labelled decimal inputs. | PARTIAL | `LaundryBooking.tsx`; web production build; Playwright smoke | Runtime-test common booking timings and touch/keyboard behavior at required widths. |
| Workspace mode runtime | Electron native workspace status remains authoritative; browser-served React now falls back to the server's explicit `/api/workspace/status` instead of silently assuming production, and the demo badge/seeded queue were verified at runtime. | EPIC_EXTENSION | `api.ts`; `AuthGate.tsx`; `LaundryShell.tsx`; workspace-mode self-test | Keep desktop and control-plane workspace selection aligned when real marketplace transport is introduced. |
| Privacy / security | RBAC, loopback binding, redacted diagnostics and backups exist. Marketplace event replay protection and fail-closed provider webhook verification now exist locally; cloud device auth, scoped cloud transport and privacy workflows remain absent. | PARTIAL | auth / diagnostics / marketplace modules | Implement protected device credentials, cloud authorization, vendor-scoped contracts and privacy workflows with the real control plane. |
| Performance | Main store-order search is SQL-backed and paged with a maintained FTS5 projection and optional keyset cursor. Global search uses bounded SQL/FTS result sets across customers, orders/invoices, garment tags, and containers. A disposable 100k-customer/500k-order fixture measured 330.15 ms for the first page, 823.49 ms for a deep offset page, 329.79 ms for the equivalent cursor page, and 500.73 ms for customer-name search; update/restart behavior has a focused regression test. | NEEDS_VERIFICATION | migrations 30–34; `listLaundryOrderPage`; `searchLaundryWorkspace`; `benchmark:v4-performance`; `test:order-search`; authenticated E2E | Optimize production-path FTS write cost and add startup/tag/print/report/render/cloud-catch-up measurements. |
| Release | Internal unsigned build distinction and signature verification exist. New Windows CI job has not run remotely; production signing/update feed unavailable. | PENDING_REMOTE_CI / EXTERNAL_BLOCKER | `.github/workflows/ci.yml`, desktop | Test local packaging/launch where possible; retain truthful production gate. |
| Hardware / providers | Keyboard scanner/native print intents exist. | NEEDS_REAL_HARDWARE / NEEDS_PROVIDER | hardware module | Keep evidence-based state; validate with real equipment/credentials. |

Latest local evidence: complete server regression set passed (typecheck/build, clean DB bootstrap, workspace separation/status, marketplace sync/API/order truth/settlement/cash/notifications/provider events, tax/invoice, catalogue/assets, legacy laundry, recovery/normalization, and authenticated E2E); focused payment, reconciliation, invoice-snapshot and typecheck regressions passed after canonical receipt snapshots were added; web build, accessibility audit and Playwright smoke passed after the operator-access correction; desktop workspace/recovery/signature/manifest checks passed; Windows unsigned installer/portable build and 3,722-entry manifest were generated; the installed executable is running and contains the production tax guard, recurring-invoice canonical bridge, stricter marketplace envelope validation, distinct sync-conflict visibility, POS Product/HSN canonical invoice bridging, and canonical local receipt rendering. This is local evidence only—remote CI remains pending because no V4 branch was pushed.

Execution order: (1) finish clean baseline evidence, (2) availability + request/intake/reassessment, (3) settlement, (4) tax/invoice, (5) UI/document/runtime/performance/security audit, (6) release evidence.
