# Release readiness

Current status: **not a production candidate**.

Passed locally in this V4 work session: the complete current server CI test list (including the formerly failing `test:ops`), clean database bootstrap regression, migration/catalogue/garment-asset checks, normalized marketplace catalogue mapping/publication and dashboard-projection assertions, marketplace external-order/settlement global-search assertions, server typecheck/build, SQL-backed order pagination, bounded global search, and FTS search/update/restart/cursor regressions, production-tax-profile fail-closed regression, event-derived customer-status/timeline and API regression, pickup schedule/collection and aggregate-version regressions, customer privacy export/correction/erasure domain and API regressions, signed customer-portal token/read-only access regression, owner privacy controls in the customer work card, marketplace device enrollment/registration/activation-gate/revocation API regression, a disposable 100k-customer/500k-order benchmark, web build, static accessibility audit, Playwright smoke aligned to the supported demo-to-production workspace transition, Sync Status cockpit build/package evidence, desktop workspace/recovery/signature/manifest checks, Windows unsigned packaging, installed launch, and responsive/contact-sheet runtime inspection. The complete server suite was rerun after POS canonical bridging and passed; focused payment, reconciliation, authenticated E2E, invoice-snapshot, settlement-batch and typecheck regressions also pass after canonical Payment Receipt/Refund Receipt snapshots and provider-evidence-gated payout attempts were added. The internally labelled release manifest for functional source `cee2023` verifies 3,736 entries, and the installed package contains the Sync Status cockpit, bounded global search including marketplace external orders and settlement evidence, invoice-name FTS backfill through migration 34, the production tax guard, the recurring-invoice canonical bridge, POS Product/HSN canonical invoice bridging, canonical local receipt rendering, settlement-batch/payout APIs, customer-status API/UI, pickup workflow API/UI, the customer privacy and signed portal modules, the customer work-card privacy controls, and the latest marketplace dashboard/catalogue modules after the final reinstall. The production-release prerequisite test passes and an unconfigured production build fails closed. The latest benchmark measured 330.15 ms for the first page, 823.49 ms for a deep offset page, 329.79 ms for the equivalent keyset page, and 500.73 ms for customer-name search; startup/tag/print/report/render coverage remain open. The most recent remote `main` CI run for baseline SHA `72767269e83d905c7f484d8ce08ae7d32a27b1bc` failed at `test:ops`; local fixes have not been pushed and therefore cannot be called green remotely.

Latest local increment: migration 36 adds explicit, store-scoped marketplace customer links and Customer 360 projection. The owner-only API/UI is idempotent, collision-protected, audited and revocable; no automatic name/phone merge is used. The focused `test:marketplace-customer-links` passed. This remains local edge functionality until the real marketplace account-link contract is available.

Current source checkpoint for this increment: local `main` commit `094c9dd`. The web/server application bundle and fresh Windows NSIS/portable package were rebuilt from this commit. The installed executable launched successfully from the current-user Epic Laundry path; the unpacked executable points at the current finance bundle and the release manifest verifies 3,756 entries. Post-login visual verification remains a manual sign-in handoff.

Package evidence: NSIS installer SHA-256 `31BE7E1F8C0E1258A8B685AA47C74DB3EC7BF620D2BB2581C4E29F6B796F9491`; portable executable SHA-256 `01F5EDDC7F77578BF8B84ADD8EB8710B24C21A1C34EC9589A77BE55FB8FA771F`; unpacked executable SHA-256 `CC0E51FB38BCC920654AEA3144476EBC1838FF1303C538FDF2C7DAEA8E7DEF6A`. Executable signing remains intentionally disabled for this developer/internal build.

Missing or partial: real marketplace cloud contract/transport (the adjacent backend is verified reference only), complete legacy GSTR/debit-note wiring through every document path, provider-backed settlement-batch/payout documents and receipt evidence, Windows package/launch CI on remote main, production signing, trusted update feed, real hardware validation, provider credentials, deep-paging/startup/tag/print/report/render performance optimization, remaining visual-owner review, and legal/CA approvals. Official GST/IRP source references for the ₹5 crore e-invoice threshold and the ₹10 crore+ 30-day reporting restriction are recorded in `TAX_COMPLIANCE_ARCHITECTURE.md`; they do not replace business-specific applicability validation.
## 2026-09-03 demo fixture refresh

The installed internal demo build includes an idempotent `DEMO-DATA-V4.1` expansion. On a clean demo workspace, and on the preserved installed demo database, the seed produces:

- 60 laundry orders across counter, customer app, website and marketplace channels;
- 52 immutable canonical invoice snapshots with line-level SAC/tax evidence;
- 24 management expenses;
- 18 TDS and 20 TCS transactions;
- 8 prepared statutory-return snapshots (prepared only; no filing evidence is fabricated);
- 8 quality claims and 6 return cases;
- 12 marketplace order projections, 6 settlement statements and 2 immutable cash collections;
- route, availability, catalogue-mapping and pending-sync fixtures.

The seed is guarded by a tenant-scoped version marker and was verified across a full stop/start cycle with stable counts. The fixture is demo-only and does not configure a real legal entity, provider, IRN, filing, payout or production credential.
