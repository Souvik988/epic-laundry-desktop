# Release readiness

Current status: **not a production candidate**.

Passed locally in this V4 work session: the complete current server CI test list (including the formerly failing `test:ops`), clean database bootstrap regression, migration/catalogue/garment-asset checks, server typecheck/build, SQL-backed order pagination, bounded global search, and FTS search/update/restart/cursor regressions, production-tax-profile fail-closed regression, event-derived customer-status/timeline and API regression, a disposable 100k-customer/500k-order benchmark, web build, static accessibility audit, Playwright smoke, Sync Status cockpit build/package evidence, desktop workspace/recovery/signature/manifest checks, Windows unsigned packaging, installed launch, and responsive/contact-sheet runtime inspection. The complete server suite was rerun after POS canonical bridging and passed; focused payment, reconciliation, authenticated E2E, invoice-snapshot, settlement-batch and typecheck regressions also pass after canonical Payment Receipt/Refund Receipt snapshots and provider-evidence-gated payout attempts were added. The current internally labelled release manifest verifies 3,727 entries, and the installed package contains the Sync Status cockpit, bounded global search, invoice-name FTS backfill through migration 34, the production tax guard, the recurring-invoice canonical bridge, POS Product/HSN canonical invoice bridging, canonical local receipt rendering, settlement-batch/payout APIs, and the customer-status API/UI. The production-release prerequisite test passes and an unconfigured production build fails closed. The latest benchmark measured 330.15 ms for the first page, 823.49 ms for a deep offset page, 329.79 ms for the equivalent keyset page, and 500.73 ms for customer-name search; startup/tag/print/report/render coverage remain open. The most recent remote `main` CI run for baseline SHA `7276726` failed at `test:ops`; local fixes have not been pushed and therefore cannot be called green remotely.

Missing or partial: real marketplace cloud contract/transport (the adjacent backend is verified reference only), complete legacy GSTR/debit-note wiring through every document path, provider-backed settlement-batch/payout documents and receipt evidence, Windows package/launch CI on remote main, production signing, trusted update feed, real hardware validation, provider credentials, deep-paging/startup/tag/print/report/render performance optimization, remaining visual-owner review, and legal/CA approvals. Official GST/IRP source references for the ₹5 crore e-invoice threshold and the ₹10 crore+ 30-day reporting restriction are recorded in `TAX_COMPLIANCE_ARCHITECTURE.md`; they do not replace business-specific applicability validation.
